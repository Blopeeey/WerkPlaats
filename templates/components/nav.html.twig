{% set navOptions = [
    {"text": "Home", "link": path('home')},
    {"text": "Over de DIF", "link": path('about')},
    {"text": "Vragenlijst", "link": path('fillsurvey')},
]
%}

{% if navType is defined %}
    {% if navType == "header"%}
        <nav id="navigation" class="navigation navigation--nonscrolled" role="navigation">
            <div>
                <a href="{{ path('home') }}"><img class="logo" id="navlogo" src="{{ asset('images/logo-blacktext.png') }}" alt="logo"></a>
            </div>
            <div id="hamburger" class="hamburger hamburger--nonscrolled"></div>
            <div id="navMenu" class="navmenu navmenu-nonscrolled navmenu--visible">
                {% for option in navOptions %}
                    <a class="navoption navoption--nonscrolled" href="{{ option.link }}">{{option.text}}</a>
                {% endfor %}
                {% if app.user %}
                    <a class="navoption navoption--nonscrolled" href="{{ path('fos_user_security_logout') }}">Log out</a>
                {% else %}
                    <a class="navoption navoption--nonscrolled" href="{{ path('fos_user_security_login') }}">Log in</a>
                    <a class="navoption navoption--nonscrolled" href="{{ path('fos_user_registration_register') }}">Register</a>
                {% endif %}
            </div>
        </nav>
    {% else %}
        <nav id="navigation" class="navigation navigation--scrolled" role="navigation">
            <div>
                <a href="{{ path('home') }}"><img class="logo" id="navlogo" src="{{ asset('images/logo-blacktext.png') }}" alt="logo"></a>
            </div>
            <div id="hamburger" class="hamburger hamburger--scrolled"></div>
            <div id="navMenu" class="navmenu navmenu--scrolled navmenu--visible">
                {% for option in navOptions %}
                    <a class="navoption navoption--scrolled" href="{{ option.link }}">{{option.text}}</a>
                {% endfor %}
                {% if app.user %}
                    <a class="navoption navoption--scrolled" href="{{ path('fos_user_security_logout') }}">Log out</a>
                {% else %}
                    <a class="navoption navoption--scrolled" href="{{ path('fos_user_security_login') }}">Log in</a>
                    <a class="navoption navoption--scrolled" href="{{ path('fos_user_registration_register') }}">Register</a>
                {% endif %}
            </div>
        </nav>
    {% endif %}
{% else %}
    <nav id="navigation" class="navigation navigation--scrolled" role="navigation">
        <div>
            <a href="{{ path('home') }}"><img class="logo" id="navlogo" src="{{ asset('images/logo-blacktext.png') }}" alt="logo"></a>
        </div>
        <div id="hamburger" class="hamburger hamburger--scrolled"></div>
        <div id="navMenu" class="navmenu navmenu--scrolled navmenu--visible">
            {% for option in navOptions %}
                <a class="navoption navoption--scrolled" href="{{ option.link }}">{{option.text}}</a>
            {% endfor %}
            {% if app.user %}
                <a class="navoption navoption--scrolled" href="{{ path('fos_user_security_logout') }}">Log out</a>
            {% else %}
                <a class="navoption navoption--scrolled" href="{{ path('fos_user_security_login') }}">Log in</a>
                <a class="navoption navoption--scrolled" href="{{ path('fos_user_registration_register') }}">Register</a>
            {% endif %}
        </div>
    </nav>
{% endif %}